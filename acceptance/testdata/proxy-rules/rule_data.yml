---
rule_data:
  proxy_enabled_purl_types:
  - npm
  - rpm

  allowed_proxy_url_patterns:
    npm:
    - ^https://artifact-registry-proxy\.caching\.svc\.cluster\.local/repository/npm-proxy.*
    rpm: []
